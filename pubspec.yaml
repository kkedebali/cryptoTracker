name: cryptotrack
description: "A new Flutter tracking project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  hive_flutter: ^1.1.0
  shared_preferences: ^2.5.5
  flutter_bloc: ^9.1.1
  dio: ^5.11.1
  intl: ^0.20.3
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.6
  analyzer: ^4.6.0

flutter:
  uses-material-design: true